This is the code in JQuery used to make my navbar menu shrink when the user of
the site scrolls to a certian point on the page.

$(window).scroll(function() {
    if ($(document).scrollTop() > 350) {
        $('nav').addClass('shrink');
        
    }
    else {
        $('nav').removeClass('shrink');
    }
});
